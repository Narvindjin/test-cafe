import styled from "styled-components";

export const StyledText = styled.span`
  background: ${(props) => props.theme.colorLightBackground};
  border-radius: 8px;
  padding: 8px 12px 8px 12px;
`;
